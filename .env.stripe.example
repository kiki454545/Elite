# Stripe Configuration
# Obtenir ces clés depuis https://dashboard.stripe.com/apikeys

# Clé publique Stripe (côté client)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Clé secrète Stripe (côté serveur - GARDER SECRET!)
STRIPE_SECRET_KEY=sk_test_...

# Secret de signature webhook Stripe
# Obtenir depuis https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=whsec_...

# URL du site pour les redirections après paiement
NEXT_PUBLIC_SITE_URL=http://localhost:3000
